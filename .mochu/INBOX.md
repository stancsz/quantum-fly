# Human inbox

## 2026-09-11 model-quality-cost M2

The matched comparison needs an independently available baseline model. The selected advertised `gpt-6-astra` arm completed 2/3 fixed tasks and returned HTTP 503 for the third, while the candidate completed 3/3. The receipt is `outputs/model-quality-cost-baseline.json`, with status `incomplete_provider_evidence`.

Decision needed before M2 can resume: provide or authorize a reliably available independent baseline endpoint/model, or explicitly accept a different baseline class. Until then, R6 remains unverified and no quality/cost advantage is claimed.
